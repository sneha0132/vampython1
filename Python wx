#Typecasting convert one data type to another data type

price= 90.45
print(price)
print(type(price))

#convert to integer
payPrice = int(price)
print(payPrice)
print(type(payPrice))

#convert integer to string
amount= 2599
stringAmount= str(amount)
print(stringAmount, "and data type is" ,type(stringAmount))

#convert string into int
# gender= "female"
#cntrl + / = to comment
#genderIntoInt = int(gender)
#print(gender, "data type is", genderIntoInt)
#string can never be convert into integer bcz string doesn't have ascii no.

#to take the input from user c language scanf
myName = input("Enter your name")
#input function has default return type as str
myAge= int(input("Enter your age"))
print("MY Name is ", myName , "and age is" , myAge)

#find the age in years when born year and current year given by user
print("current year - born year")

#currency convertor ruppes to USD :- 1 usd = 84rs
